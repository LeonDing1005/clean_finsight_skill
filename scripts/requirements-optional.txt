# Optional: Chinese A-share + HK market data
akshare>=1.12
jieba>=0.42
efinance>=0.5
sogou-search>=0.1

# Optional: Browser automation
playwright>=1.40
fake-useragent>=1.4

# Optional: PDF export from DOCX (requires Microsoft Word on Windows)
docx2pdf>=0.1
